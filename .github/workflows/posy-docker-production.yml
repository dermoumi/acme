name: Posy docker (production)

# Separate from posy-docker.yml: paths and tags in one push block silently
# skips the tag runs.
on:
  push:
    tags: ["posy-v*"]

jobs:
  verify:
    permissions:
      contents: read
    uses: ./.github/workflows/target-docker.yml
    with:
      app: posy

  publish:
    needs: verify
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/target-docker-publish.yml
    with:
      app: posy
