coverage:
  status:
    # No baseline yet, so any target would be invented; flip to `target` later.
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

# Path-based attribution from the single upload; no statuses, so no extra checks.
component_management:
  individual_components:
    - component_id: posy
      name: apps/posy
      paths:
        - apps/posy/**
    - component_id: app
      name: "@acme/app"
      paths:
        - packages/app/**
    - component_id: assets
      name: "@acme/assets"
      paths:
        - packages/assets/**
    - component_id: db
      name: "@acme/db"
      paths:
        - packages/db/**
    - component_id: deploy
      name: "@acme/deploy"
      paths:
        - packages/deploy/**
    - component_id: health
      name: "@acme/health"
      paths:
        - packages/health/**
    - component_id: rate-limiter
      name: "@acme/rate-limiter"
      paths:
        - packages/rate-limiter/**
    - component_id: sentry
      name: "@acme/sentry"
      paths:
        - packages/sentry/**

comment:
  layout: condensed_header, components, diff, files
  require_changes: false

# Mirrors the `coverage.exclude` lists in the per-package vitest configs.
ignore:
  - "**/testing/**"
  - "**/fixtures/**"
  - "**/*.config.ts"
  - apps/posy/src/server/index.node.ts
  - packages/tsconfig/**
