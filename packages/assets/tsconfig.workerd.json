{
  "extends": "@acme/tsconfig/tsconfig.workerd.json",
  "compilerOptions": {
    // Resolve #assets the way workerd does, so node-only imports stay out.
    "customConditions": ["workerd"]
  },
  "include": ["src"],
  "exclude": ["**/*.node.ts", "**/*.node.test.ts"]
}
