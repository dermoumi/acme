services:
  app:
    build:
      context: ../..
      dockerfile: apps/posy/Dockerfile
    image: posy:local
    init: true
    ports:
      - "3000:3000"
    volumes:
      - posy-data:/var/lib/posy
    restart: unless-stopped

volumes:
  posy-data:
