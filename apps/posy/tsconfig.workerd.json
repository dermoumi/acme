{
  "extends": "@acme/tsconfig/tsconfig.workerd.json",
  "compilerOptions": {
    // Resolve #testing/runtime the way the pool does, so node-only code
    // (better-sqlite3 pulls in an ambient node types reference) stays out.
    "customConditions": ["workerd"]
  },
  "include": ["src/server"],
  "exclude": ["**/*.node.ts"]
}
