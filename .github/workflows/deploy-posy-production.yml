name: Deploy Posy (production)

on:
  push:
    tags: ["posy-v*"]

jobs:
  production:
    permissions:
      contents: read
    uses: ./.github/workflows/app-deploy.yml # zizmor: ignore[secrets-inherit]
    with:
      app: posy
      environment: production
      health-url: https://play.posy.workers.dev/health
    secrets: inherit
